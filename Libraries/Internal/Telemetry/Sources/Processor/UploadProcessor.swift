//
// Copyright © 2025 TruVideo. All rights reserved.
//

/*
import CommonUtilities
import Foundation
import Networking

final class UploadProcessor: TelemetryManagerSubscriber {
    // MARK: - Private Properties

    private let session: Session

    // MARK: - Initializer

    init(session: Session) {
        self.session = session
    }

    // MARK: - TelemetryManagerSubscriber

    func didReceive(_ report: TelemetryReport) {
        process(report)
    }

    // MARK: - Private methods

    private func process(_ report: TelemetryReport) {
        Task {
            do {
                // Send the Report to HTTP
            } catch {
                //try await storage.write(report)
            }
        }
    }
}
*/
